#![deny(missing_docs, unsafe_op_in_unsafe_fn, rust_2018_idioms)]
#![forbid(clippy::dbg_macro, clippy::print_stdout)]

//! Varta performance harness — Session 06 will land the bench subcommands
//! (`latency`, `cpu-50-agents`, `binary-size`) here. The current placeholder
//! is a no-op so the workspace builds and the bin slot is reserved.

fn main() {}
