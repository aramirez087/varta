#![deny(missing_docs, unsafe_op_in_unsafe_fn, rust_2018_idioms)]
#![forbid(clippy::dbg_macro, clippy::print_stdout)]

//! Varta end-to-end test harness — Session 06 will land integration fixtures
//! here. The crate currently exposes no public items; tests live under
//! `tests/` once Session 06 wires them up.
